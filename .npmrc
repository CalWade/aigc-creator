# pin 精确版本，避免 ^ ~ 漂移
save-exact=true

# 严格 peer deps：依赖声明的 peer 不满足时拒绝安装，不静默
strict-peer-dependencies=true

# 缺 peer 时自动装上，省掉手动 pnpm add -D
auto-install-peers=true

# 不把子包依赖提升到根 node_modules，避免子包隐式访问别的子包依赖
shamefully-hoist=false
